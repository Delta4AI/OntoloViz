window.dccFunctions = window.dccFunctions || {};
window.dccFunctions.hexColorToToolTip = function(value) {
  // document.querySelectorAll('.rc-slider-mark-text').forEach((m) => {m.style.color = m.innerHTML});
  return (value + "%");
}